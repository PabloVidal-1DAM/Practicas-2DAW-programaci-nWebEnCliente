// importar la función que ayudará a hacer el ejercicio
import {calcularPotencia} from "./biblioteca.js";

let base = 2;
let exponente = 2;

console.log("El resultado de base: " +base+" y exponente: "+exponente+ "da: "+calcularPotencia(base,exponente));